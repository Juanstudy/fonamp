name: Release APK

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17 (Corretto)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Restore release keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > /tmp/fonamp-release.jks

      - name: Assemble signed release APK (R8 + shrink)
        env:
          FONAMP_STORE_FILE: /tmp/fonamp-release.jks
          FONAMP_STORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          FONAMP_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          FONAMP_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease

      - name: Check release APK size (<40MB)
        run: |
          APK=app/build/outputs/apk/release/app-release.apk
          ls -lh app/build/outputs/apk/release/
          SIZE=$(stat -c%s "$APK")
          echo "release APK: $SIZE bytes ($(numfmt --to=iec "$SIZE"))"
          MAX=$((40 * 1024 * 1024))
          if [ "$SIZE" -ge "$MAX" ]; then
            echo "FAIL: release APK exceeds 40MB"
            exit 1
          fi

      - name: Rename APK
        run: cp app/build/outputs/apk/release/app-release.apk fonamp-${{ github.ref_name }}.apk

      - name: Publish to GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          files: fonamp-${{ github.ref_name }}.apk
          generate_release_notes: true
