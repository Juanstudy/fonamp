package com.fonamp.core.database

/**
 * Slice A scaffold: proves Room + KSP wiring resolves.
 * FonampDatabase v1 + DAOs land in Slice D.
 */
object DatabaseScaffold {
    const val NAME = "fonamp.db"
    const val VERSION = 3
}
