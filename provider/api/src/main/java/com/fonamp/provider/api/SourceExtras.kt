package com.fonamp.provider.api

/** Extras bundle keys owned by `provider/api` (design §2). The player reads only these. */
object SourceExtras {
    const val KEY_SOURCE_ID = "source_id"
    const val KEY_STABLE_ID = "stable_id"
    const val KEY_IS_LIVE = "is_live"
    const val KEY_STATION_UUID = "station_uuid"
    const val KEY_COUNTRY = "country"
}
